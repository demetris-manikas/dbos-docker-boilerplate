{{ $config := .Values.keycloak }}

{{ $keycloakHeadlessCtx := dict
  "name" (printf "%s-headless" $config.name)
}}
{{ $keycloakHeadlessServiceName := include "template.service.name" $keycloakHeadlessCtx }}

{{ $ctx := dict
  "name" $config.name
}}

{{ $keycloakServiceName := include "template.service.name" $ctx }}

apiVersion: v1
kind: Service
metadata:
  name: {{ $keycloakHeadlessServiceName }}
spec:
  clusterIP: None
  selector:
    service: {{ $keycloakServiceName }}
