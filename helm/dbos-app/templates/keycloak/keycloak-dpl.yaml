{{ $config := .Values.keycloak }}

{{ $ctx := dict
  "name" $config.name
  "registry" .Values.registry
  "deployment" $config.deployment
  "port" $config.service.ports.keycloak.port
}}

{{ include "template.dpl" $ctx }}