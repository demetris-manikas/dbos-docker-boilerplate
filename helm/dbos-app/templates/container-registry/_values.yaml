container_registry:
  enabled: false
  name: container-registry
  deployment:
    revisionHistoryLimit: 5
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  resources:
    requests:
      memory: "100Mi"
      cpu: "50m"
    limits:
      memory: "2Gi"
      cpu: 2
