
{{ $config := .Values.backend }}

{{ $ctx := dict
  "name" $config.name
  "registry" .Values.registry
  "deployment" $config.deployment
  "port" $config.service.ports.backend.port
}}

{{ include "template.dpl" $ctx }}
