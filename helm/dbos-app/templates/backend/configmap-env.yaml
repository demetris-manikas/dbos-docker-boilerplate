apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "template.configmap-env.name" .Values.backend.name }}
data:
  APP_DB_HOSTNAME: {{ include "template.service.name" .Values.backend_db.name }}
  BROKERS: {{ .Values.backend.env.brokers }}