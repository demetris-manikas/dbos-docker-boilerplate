apiVersion: v1
kind: PersistentVolume
metadata:
  name: dbos-app-dev

  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadOnlyMany
  hostPath:
    path: "/home/demetris/projects/dbos-docker-tpl/app"
